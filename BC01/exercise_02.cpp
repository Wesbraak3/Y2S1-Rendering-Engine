#include <cstdio>

/*
Exercise 1:
1. Write a C++ program with one regular variable A and one pointer to that variable PA. Print the value of both variables using printf. What are their results?
2. Using the same program as before, change the values of both the variables A and PA and print their values using printf. What are their results?
3. Try now creating a third pointer PB that points to A, and experiment with changing PB to another value. Print the values of A, PA and PB using printf. What are their results?
4. Make two functions and call them in the main program: one receives a regular variable as a parameter (int P), and the other receives a pointer (int* PP). The functions should multiply the parameter by 2 and not return the result. Use the variable A from the previous questions as an argument for both functions and print the value of A using printf after each function cal
*/

void exercise_02() {

}